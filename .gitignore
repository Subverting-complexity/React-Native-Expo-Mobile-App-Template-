# Node
node_modules/
npm-debug.*
yarn-debug.*
yarn-error.*

# Expo
.expo/
dist/
web-build/

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# macOS
.DS_Store
*.pem

# Local env files — never commit real tokens. Holds EXPO_TOKEN (per-repo Expo
# auth); .env.example is the committed, token-less template.
.env
.env*.local
!.env.example

# TypeScript
*.tsbuildinfo

# Test coverage
coverage/

# Local Claude Code / AI-assistant state (not part of the template)
.claude/
