# Node
node_modules/
npm-debug.*
yarn-debug.*
yarn-error.*

# Expo
.expo/
dist/
web-build/

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# macOS
.DS_Store
*.pem

# Local env files — never commit real tokens. Holds EXPO_TOKEN (per-repo Expo
# auth); .env.example is the committed, token-less template.
.env
.env*.local
!.env.example

# TypeScript
*.tsbuildinfo

# Test coverage
coverage/

# github-workflow plugin scratch files
.claude/plan.md
.claude/claim-*.sha

# graphify — generated artifacts (rebuild via `graphify . --update`, ~10s, 0 tokens)
graphify-out/graph.json
graphify-out/graph.html
graphify-out/cost.json
graphify-out/.graphify_analysis.json
graphify-out/.graphify_python
graphify-out/.graphify_root
